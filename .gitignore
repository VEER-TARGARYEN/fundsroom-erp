node_modules
dist
dist-ssr
*.local
.vite

# Secrets & env — never commit real values (keep .env.example)
.env
.env.*
!.env.example

# Private operations runbook — holds live credentials. This repository is
# PUBLIC, so these patterns must stay above any doc glob that might catch them.
PRIVATE-RUNBOOK.md
*.private.md
secrets/

# Build artifacts
*.tsbuildinfo

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
